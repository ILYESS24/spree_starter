#!/bin/bash
# Wrapper pour Rails qui évite le segfault de Ruby 3.3.0 sur Koyeb
# Solution: utiliser ruby directement avec un chargement minimal

# Exécuter ruby avec un chargement minimal pour éviter le segfault
# Le problème vient du shape cache dans rubygems.rb
exec /usr/local/bin/ruby -W0 "$@"

